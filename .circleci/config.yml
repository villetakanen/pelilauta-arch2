version: 2.1

workflows:
  version: 2
  build:
   jobs:
     -build

jobs:
  build:
    docker:
      -image: circleci/node:10.16.3
    steps:
      -run:
        name: 'install npm packages'
        command: 'npm install'
      -run:
        name: 'build'
        command: 'npm run build'